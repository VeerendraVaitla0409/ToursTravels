const BASE_URL = "http://localhost:8080";

const ENDPOINTS = {
    ADD_ENQUIRY: `${BASE_URL}/enquiries/add`,
    GET_SERVICES: `${BASE_URL}/services`,
    GET_PACKAGES: `${BASE_URL}/packages`
  };

  export {BASE_URL, ENDPOINTS} 